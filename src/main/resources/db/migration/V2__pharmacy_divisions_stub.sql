-- ЗАГОТОВКА НА БУДУЩЕЕ (вне текущего скоупа): лист "Дивизионы" недельного отчёта требует
-- маппинга аптека -> дивизион -> директор, которого сейчас нет ни в одном источнике данных.
-- Таблица создаётся, чтобы зафиксировать структуру, но намеренно не заполняется данными.
-- TODO: определить источник данных для маппинга (внешний файл/справочник/ручной ввод)
-- и загрузить/поддерживать актуальность этой таблицы, прежде чем реализовывать лист "Дивизионы".
CREATE TABLE pharmacy_divisions (
    pharmacy_code VARCHAR(64) PRIMARY KEY,
    division_name VARCHAR(255) NOT NULL,
    director_name VARCHAR(255) NOT NULL
);
